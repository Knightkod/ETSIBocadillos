<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>
    -->
        <!-- Customize your theme here. -->
    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base"> <!--cuidado!, es solo para que adapte esto o que valga lo mismo el de values-v21-->
    </style>

    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!--si esto no esta a true casca salvo que luego
        metas una toolbar a mano en las layouts y tal.
        nosotros hacemos eso y por eso arriba le decimos que NO carge action bar-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

</resources>
